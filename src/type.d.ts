import { PressableProps, StyleProp } from "react-native"

export type StyledCompsProps = {
    cN?: string
    style?: StyleProp
}

export type PressableCompsProps = {
    pressOptions?: PressableProps
}